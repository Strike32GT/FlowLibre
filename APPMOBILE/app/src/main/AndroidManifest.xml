<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--Permisos de Media(para el miracast)-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_flowlibre_mobile_"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_flowlibre_mobile__round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlowLibre">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FlowLibre">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Servicio MediaSession-->
        <service
            android:name=".presentation.services.MediaSessionService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>


        <!--Servicio que recibe el MediaSession-->
        <receiver
            android:name=".presentation.receivers.MediaReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.mas.flowlibre.PLAY_PAUSE" />
                <action android:name="com.mas.flowlibre.NEXT" />
                <action android:name="com.mas.flowlibre.PREVIOUS" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>

</manifest>